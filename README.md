# TCP (Transmission Control Protocol) chat application in C

Multiple clients can connect to the server, when a client sends message to server it wil be broadcasted to all theclients connected to the server
